#include "MultiLayerPerceptron.h"
#include "Math.h"
#include <numeric>

int main(void) {
	MultiLayerPerceptron* nnet = new MultiLayerPerceptron(4, 5, 4);

	//nnet->predict(x);

	while (1);
	return 0;
}